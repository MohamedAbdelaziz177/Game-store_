package com._Abdelaziz26.Game.Mappers;

import org.mapstruct.Mapper;

@Mapper
public interface EventImageMapper {

    //@Mapping(target = "imageUrl", source = "image")
    //EventImage toEntity(MultipartFile image);
//
    //default List<EventImage> mapImages(List<MultipartFile> images) {
    //    return images.stream()
    //            .map(this::toEntity)
    //            .collect(Collectors.toList());
    //}
}
